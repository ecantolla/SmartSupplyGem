/** @type {import('next').NextConfig} */
const nextConfig = {
  // No se necesita ninguna configuración especial aquí.
  // Next.js y Vercel funcionan mejor con la configuración por defecto
  // para aplicaciones dinámicas como la tuya.
};

export default nextConfig;